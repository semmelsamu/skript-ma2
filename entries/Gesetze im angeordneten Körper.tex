\begin{enumerate}
    \item $a < b, b < c \Longrightarrow a < c$ (Transitivität)
    \item $a < v \Longleftrightarrow a + c < b + c$
    \item $a < b, c > 0 \Longrightarrow ca < cb$
    \item $a < b < 0 \Longrightarrow \frac{1}{a} < \frac{1}{b}$
    \item $0 \leq a \leq b \Longrightarrow a^2 < b^2$
    \item $\forall a \neq 0 : a^2 > 0$ (insbesondere $1 = 1 \cdot 1 = 1^2 > 0$)
\end{enumerate}