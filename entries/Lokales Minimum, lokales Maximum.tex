Sei $f: D \subseteq \R \to \R$ eine Funktion. Man sagt: $f$ hat im Punkt $p \in D$ ein \term{lokales Minimum} (bzw. \term{lokales Maximum}), wenn es ein $\varepsilon > 0$ gibt wo für alle $x$ aus einer Umgebung $D \cap U_\varepsilon(p)$ gilt:
$$f(x) \leq f(p) \qquad \text{bzw.} \qquad f(x) \geq f(p).$$