Wenn die Ableitung $f'$ einer differenzierbaren Funktion $f$ wieder differenzierbar ist, so bezeichnet man
$$f'' := (f')'$$
als \term{zweite Ableitung} von $f$. Allgemein gilt für alle $n \in \N$:
$$f^{(n)} := (f^{(n-1)})', \qquad f^{(0)} := f$$