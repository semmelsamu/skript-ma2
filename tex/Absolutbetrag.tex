In jedem angeordneten Körper $K$ kann man einen \term{Absolutbetrag} einführen: Für $a \in K$ definiert man

$$|a| := \begin{cases} 
a & \text{für $a \geq 0$}\\ 
-a & \text{für $a < 0$}
\end{cases}
$$